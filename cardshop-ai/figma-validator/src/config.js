/** @type {import('./types.js').ValidatorConfig} */
export const config = {
  tolerances: {
    fontSizePx: 1,
    fontWeight: 100,
    dimensionPct: 10,
    colorExact: true,
  },
  output: {
    html: "report.html",
    json: "report.json",
  },
};
